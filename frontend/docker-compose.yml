name: my-frontend-compose

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    command: ["pnpm", "--filter", "frontend", "dev"]
    ports:
      - "4173:4173"
    volumes:
      - .:/app
      - /app/node_modules/